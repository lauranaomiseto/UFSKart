- função escrever_registro_corredor: a lógica pra posição da barra não está diferente da do exemplo de arquivo de dados no doc?

- função busca_binaria_com_reps: o que é o parâmetro int retorno_se_nao_encontrado?

- [c] funcção cadastrar_corredor_menu: preciso inicializar as strings dos veículos?

- [c] para realização da busca binária, levando em consideração as funções de comparação elaboradas, é necessário transferir a chave de busca para uma struct adequada e utilizar essa struct como argumento da função, ou é correto passar a chave diretamente? (para verificação de chaves repetidas nas funções de cadastro, utilizei a chave diretamente, mas nas funções busca, a implementação veio com a passagem das chaves para a struct)

- função liberar_espaco_menu: a atualização do arquivo de índice deve ser feita nessa própria função ou posso chamar a função de criar_corredores_idx? (abordar problema da mensagem de sucesso)

remover_corredor_menu, liberar_espaco_menu

+ correção da utilização da função busca_binaria - passagem da chave para o struct adequado, que por sua vez é utilizado como argumento key; 

