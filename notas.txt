- função escrever_registro_corredor: a lógica pra posição da barra não está diferente da do exemplo de arquivo de dados no doc?

- função busca_binaria_com_reps: o que é o parâmetro int retorno_se_nao_encontrado?

- define TAM_REGISTRO_VEICULO, PISTA, CORRIDA: porque a somatória dá um byte a mais? (+1 no final sempre)

- define TAM_REGISTRO_CORREDOR: não deveria somar +1? me parece que p delimitador ";" não foi considerado para o campo veiculo (deveria ser 14*3+3+1 = 46)

- função cadastrar_corredor_menu: não deve haver um teste para verificar se a quantidade de registros ultrapassou o MAX_REGISTROS? (a variável global contadora qtd_registros_corredores deve ser iterada conforme a adição de novos registros). Se sim, o erro apresentado pode ser o ERRO_MEMORIA_INSUFICIENTE?

- [c] funcção cadastrar_corredor_menu: preciso inicializar as strings dos veículos?



SET ARQUIVO_CORREDORES TO 'dados/corredores.dat';
SET ARQUIVO_VEICULOS TO 'dados/veiculos.dat';
SET ARQUIVO_PISTAS TO 'dados/pistas.dat';
SET ARQUIVO_CORRIDAS TO 'dados/corridas.dat';
